# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
**/.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Environment variables
.env.local
.env.development
.env.production

# Env var reference (optional: remove this line to commit ENV_VARS.md)
ENV_VARS.md

# Firebase
*firebase-adminsdk*.json
club-1111-firebase-adminsdk-43fx3-c9a39031bf.json
.env*.local

# compiled output (was committed by mistake)
/dist
audit-report/verification-links.local.txt
claims-backup-*.json

# Local design-review screenshots (not shipped)
.impeccable/review-*

# Security audit working material — contains real account/UID mappings and raw
# emulator logs. Kept local deliberately; this repo is public.
audit-report/

# Platform-specific tool binary (12 MB, macOS-arm64 only). Regenerated on
# first run by the skill launcher; no reason to carry it in git history.
.claude/skills/*/scripts/bin/
